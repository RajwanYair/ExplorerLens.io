<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="ExplorerLens.Manager.Views.PluginsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0"
                Padding="24,24,24,12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Row="0"
                    Grid.Column="0">
                <TextBlock Text="Plugins"
                        Style="{StaticResource TitleTextBlockStyle}"/>
                <TextBlock Style="{StaticResource BodyTextBlockStyle}"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                           Margin="0,4,0,0">
                    <Run Text="{x:Bind ViewModel.InstalledCount, Mode=OneWay}"/>
                    <Run Text="installed •"/>
                    <Run Text="{x:Bind ViewModel.AvailableUpdatesCount, Mode=OneWay}"/>
                    <Run Text="updates available"/>
                </TextBlock>
            </StackPanel>

            <StackPanel Grid.Row="0"
                    Grid.Column="1"
                    Orientation="Horizontal"
                    Spacing="8"
                    VerticalAlignment="Top">
                <Button Content="Install from File"
                        Command="{x:Bind ViewModel.InstallFromFileCommand}">
                    <Button.Icon>
                        <FontIcon Glyph="&#xE8E5;"/>
                    </Button.Icon>
                </Button>
                <Button Content="Refresh"
                        Command="{x:Bind ViewModel.RefreshCommand}">
                    <Button.Icon>
                        <FontIcon Glyph="&#xE72C;"/>
                    </Button.Icon>
                </Button>
            </StackPanel>

            <AutoSuggestBox Grid.Row="1"
                    Grid.Column="0"
                    Grid.ColumnSpan="2"
                            PlaceholderText="Search plugins..."
                            Text="{x:Bind ViewModel.SearchQuery, Mode=TwoWay}"
                            Margin="0,12,0,0"
                            QueryIcon="Find"/>
        </Grid>

        <!-- Content -->
        <NavigationView Grid.Row="1"
                        PaneDisplayMode="Left"
                        IsBackButtonVisible="Collapsed"
                        IsSettingsVisible="False"
                        IsPaneToggleButtonVisible="False"
                        OpenPaneLength="200">
            <NavigationView.MenuItems>
                <NavigationViewItem Content="Installed"
                        Tag="Installed"
                        Icon="Library"/>
                <NavigationViewItem Content="Marketplace"
                        Tag="Marketplace"
                        Icon="Shop"/>
                <NavigationViewItem Content="Updates"
                        Tag="Updates"
                        Icon="Download"/>
            </NavigationView.MenuItems>

            <Grid>
                <ProgressRing IsActive="{x:Bind ViewModel.IsLoading, Mode=OneWay}"
                              Width="48"
                        Height="48"
                              HorizontalAlignment="Center"
                              VerticalAlignment="Center"/>

                <Grid Visibility="{x:Bind ViewModel.IsLoading, Mode=OneWay, Converter={StaticResource InverseBoolToVisibilityConverter}}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="400"/>
                    </Grid.ColumnDefinitions>

                    <!-- Plugin List -->
                    <ListView Grid.Column="0"
                              ItemsSource="{x:Bind ViewModel.InstalledPlugins}"
                              SelectedItem="{x:Bind ViewModel.SelectedPlugin, Mode=TwoWay}"
                              SelectionMode="Single"
                              Padding="12">
                        <ListView.ItemTemplate>
                            <DataTemplate x:DataType="local:PluginInfo">
                                <Grid Padding="12"
                                        ColumnSpacing="12">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <FontIcon Grid.Column="0"
                                            Glyph="&#xE74C;"
                                            FontSize="32"
                                            VerticalAlignment="Center"/>

                                    <StackPanel Grid.Column="1"
                                            VerticalAlignment="Center">
                                        <TextBlock Text="{x:Bind Name}"
                                                Style="{StaticResource BodyStrongTextBlockStyle}"/>
                                        <TextBlock Text="{x:Bind Description}"
                                                   Style="{StaticResource CaptionTextBlockStyle}"
                                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                   TextTrimming="CharacterEllipsis"
                                                   MaxLines="2"/>
                                        <StackPanel Orientation="Horizontal"
                                                Spacing="8"
                                                Margin="0,4,0,0">
                                            <TextBlock Text="{x:Bind Vendor}"
                                                       Style="{StaticResource CaptionTextBlockStyle}"
                                                       Foreground="{ThemeResource TextFillColorTertiaryBrush}"/>
                                            <TextBlock Text="•"
                                                       Style="{StaticResource CaptionTextBlockStyle}"
                                                       Foreground="{ThemeResource TextFillColorTertiaryBrush}"/>
                                            <TextBlock Text="{x:Bind Version}"
                                                       Style="{StaticResource CaptionTextBlockStyle}"
                                                       Foreground="{ThemeResource TextFillColorTertiaryBrush}"/>
                                        </StackPanel>
                                    </StackPanel>

                                    <ToggleSwitch Grid.Column="2"
                                                  IsOn="{x:Bind IsEnabled, Mode=OneWay}"
                                                  VerticalAlignment="Center"/>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>

                    <!-- Plugin Details -->
                    <Grid Grid.Column="1"
                          Background="{ThemeResource LayerFillColorDefaultBrush}"
                          BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
                          BorderThickness="1,0,0,0"
                          Padding="24"
                          Visibility="{x:Bind ViewModel.SelectedPlugin, Mode=OneWay, Converter={StaticResource NullToVisibilityConverter}}">
                        <ScrollViewer>
                            <StackPanel Spacing="16">
                                <!-- Plugin Header -->
                                <StackPanel Spacing="8">
                                    <TextBlock Text="{x:Bind ViewModel.SelectedPlugin.Name, Mode=OneWay}"
                                               Style="{StaticResource TitleTextBlockStyle}"/>
                                    <TextBlock Text="{x:Bind ViewModel.SelectedPlugin.Vendor, Mode=OneWay}"
                                               Style="{StaticResource BodyTextBlockStyle}"
                                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                    <TextBlock Text="{x:Bind ViewModel.SelectedPlugin.Version, Mode=OneWay}"
                                               Style="{StaticResource CaptionTextBlockStyle}"
                                               Foreground="{ThemeResource TextFillColorTertiaryBrush}"/>
                                </StackPanel>

                                <!-- Description -->
                                <TextBlock Text="{x:Bind ViewModel.SelectedPlugin.Description, Mode=OneWay}"
                                           TextWrapping="Wrap"/>

                                <!-- Supported Formats -->
                                <StackPanel Spacing="8">
                                    <TextBlock Text="Supported Formats"
                                            Style="{StaticResource BodyStrongTextBlockStyle}"/>
                                    <ItemsControl ItemsSource="{x:Bind ViewModel.SelectedPlugin.Formats, Mode=OneWay}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <StackPanel Orientation="Horizontal"
                                                        Spacing="4"/>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border Background="{ThemeResource AccentFillColorDefaultBrush}"
                                                        CornerRadius="4"
                                                        Padding="8,4">
                                                    <TextBlock Text="{Binding}"
                                                               Style="{StaticResource CaptionTextBlockStyle}"
                                                               Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}"/>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>

                                <!-- Capabilities -->
                                <StackPanel Spacing="8">
                                    <TextBlock Text="Capabilities"
                                            Style="{StaticResource BodyStrongTextBlockStyle}"/>
                                    <ItemsControl ItemsSource="{x:Bind ViewModel.SelectedPlugin.Capabilities, Mode=OneWay}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding}"
                                                           Style="{StaticResource CaptionTextBlockStyle}"
                                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>

                                <!-- Statistics -->
                                <StackPanel Spacing="8"
                                            Visibility="{x:Bind ViewModel.SelectedPlugin.Statistics, Mode=OneWay, Converter={StaticResource NullToVisibilityConverter}}">
                                    <TextBlock Text="Statistics"
                                            Style="{StaticResource BodyStrongTextBlockStyle}"/>
                                    <Grid ColumnSpacing="16"
                                            RowSpacing="8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>

                                        <StackPanel Grid.Row="0"
                                                Grid.Column="0">
                                            <TextBlock Text="Total Requests"
                                                       Style="{StaticResource CaptionTextBlockStyle}"
                                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                            <TextBlock Text="{x:Bind ViewModel.SelectedPlugin.Statistics.TotalRequests, Mode=OneWay}"
                                                       Style="{StaticResource SubtitleTextBlockStyle}"/>
                                        </StackPanel>

                                        <StackPanel Grid.Row="0"
                                                Grid.Column="1">
                                            <TextBlock Text="Success Rate"
                                                       Style="{StaticResource CaptionTextBlockStyle}"
                                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                            <TextBlock Style="{StaticResource SubtitleTextBlockStyle}">
                                                <Run Text="{x:Bind ViewModel.SelectedPlugin.Statistics.SuccessfulRequests, Mode=OneWay}"/>
                                                <Run Text="/"/>
                                                <Run Text="{x:Bind ViewModel.SelectedPlugin.Statistics.TotalRequests, Mode=OneWay}"/>
                                            </TextBlock>
                                        </StackPanel>

                                        <StackPanel Grid.Row="1"
                                                Grid.Column="0"
                                                Grid.ColumnSpan="2">
                                            <TextBlock Text="Average Time"
                                                       Style="{StaticResource CaptionTextBlockStyle}"
                                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                            <TextBlock Style="{StaticResource SubtitleTextBlockStyle}">
                                                <Run Text="{x:Bind ViewModel.SelectedPlugin.Statistics.AverageTimeMs, Mode=OneWay}"/>
                                                <Run Text="ms"/>
                                            </TextBlock>
                                        </StackPanel>
                                    </Grid>
                                </StackPanel>

                                <!-- Actions -->
                                <StackPanel Spacing="8">
                                    <Button Content="Update"
                                            Command="{x:Bind ViewModel.UpdatePluginCommand}"
                                            CommandParameter="{x:Bind ViewModel.SelectedPlugin, Mode=OneWay}"
                                            HorizontalAlignment="Stretch"/>
                                    <Button Content="Uninstall"
                                            Command="{x:Bind ViewModel.UninstallPluginCommand}"
                                            CommandParameter="{x:Bind ViewModel.SelectedPlugin, Mode=OneWay}"
                                            HorizontalAlignment="Stretch"
                                            Style="{StaticResource AccentButtonStyle}"/>
                                </StackPanel>
                            </StackPanel>
                        </ScrollViewer>
                    </Grid>
                </Grid>
            </Grid>
        </NavigationView>
    </Grid>
</Page>
